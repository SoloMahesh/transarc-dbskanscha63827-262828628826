# Assets

Place your local images, icons, and other static assets here.

- `images/`: Put logo.png, hero-bg.jpg, etc. here.
- `fonts/`: Put local fonts here if not using Google Fonts.

> Note: The current layout uses remote URLs as placeholders. You can download and place them here, then update the paths in `index.html` and `css/style.css`.
